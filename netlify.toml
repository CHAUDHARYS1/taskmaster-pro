[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA fallback — React Router handles all client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
